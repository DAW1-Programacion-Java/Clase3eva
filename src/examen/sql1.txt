INSERT INTO products (productCode, productName, productLine, productScale, productVendor, productDescription, quantityStock, buyPrice, MSRP) VALUES (?,?,?,?,?,?,?,?,?);
UPDATE products SET quantityStock = quantityStock * ? WHERE productLine = ?;
SELECT * FROM products WHERE productLine IN (?, ?) AND buyPrice BETWEEN ? AND ?;